from enum import Enum

class OrganizerConstants(Enum):
    ORGANIZER = "Organizer"
    EXCLUDE = "exclude"
    KEYWORDS = "keywords"
    DATA = "data"
    CODE = "code"
    SCENE = "scene"
    RESOURCE = "Resource"
    DEFAULT = "default"